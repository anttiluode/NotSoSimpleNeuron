# Current status

The repository now has three separated mechanism gates:

- v0 spatial distributed contacts: frozen in `results/v0.json`;
- frequency/temporal addressing: frozen in `results/frequency_gate.json`;
- active local spatial interaction: frozen in `results/v1.json`.

The important negative control is that the tuned two-state point resonator beats the single-site quasi-active cable on peak/DC selectivity. The next morphology claim therefore requires the combined spatio-spectral branch-bank experiment rather than resonance alone.
