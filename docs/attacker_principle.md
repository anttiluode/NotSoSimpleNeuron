# Attacker principle

A mechanism is only credited for the capability that survives an appropriately matched simpler attacker. A negative attacker result is not a failed repository result; it narrows the claim and determines the next gate.

The frequency gate is the current example: temporal addressing survives, while the single-site morphology-specific claim does not because a tuned point resonator is stronger on the frozen selectivity metric.
