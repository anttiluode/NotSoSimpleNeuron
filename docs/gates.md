# Gate map

```text
v0 spatial contacts              PASS constructed witness
frequency temporal addressing    PASS; point attacker stronger on single-site selectivity
v1 active local interaction      PASS nominal; 1/64 stress reversal
next spatio-spectral branch bank OPEN
```

Each gate is kept separate so a later mechanism cannot retroactively rescue an earlier failed claim.
