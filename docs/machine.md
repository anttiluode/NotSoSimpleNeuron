# Working machine hypothesis

```text
small event travels
      -> spatial landing pattern
      -> local temporal dynamics
      -> local state-dependent interaction
      -> branch-state mixture
      -> later soma/AIS decision
```

The route is not assumed to carry the full high-dimensional state. The receiver supplies expansion and history. Later gates must determine whether this factorization is computationally useful compared with matched non-spatial attackers.
