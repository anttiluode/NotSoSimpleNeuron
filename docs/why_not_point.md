# Why not just use a point neuron?

Often, we should. The point model is the baseline to beat, not an opponent to exclude. The frequency gate demonstrates this directly: a tuned two-state point resonator has stronger single-site peak/DC selectivity than the quasi-active cable. Morphology only stays when spatial organization, local interaction, routing, robustness, or resource constraints buy something under a matched comparison.
